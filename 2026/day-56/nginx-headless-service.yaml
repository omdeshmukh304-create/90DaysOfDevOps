apiVersion: v1
kind: Service
metadata:
  name: nginx-headless
spec:
  clusterIP: None   # 👈 This makes it Headless
  selector:
    app: nginx      # must match StatefulSet labels (we’ll use this later)
  ports:
  - port: 80
    name: http