apiVersion: v1
kind: Pod
metadata:
  name: env-test-pod
spec:
  containers:
  - name: busybox-container
    image: busybox
    command: ["sh", "-c", "env && sleep 3600"]
    envFrom:
    - configMapRef:
        name: app-config